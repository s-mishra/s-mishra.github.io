# If Netlify builds this repo directly, build the static site with Node.
# (The live site is normally served from the gh-pages branch, which the GitHub
# Action publishes — see .github/workflows/gh-pages.yml.)
[build]
  command = "node build.mjs"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"
